import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.svitlopulse.cfg',
  appName: 'SvitloPulse',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
